package com.bootcamp.product.service;

import org.springframework.stereotype.Service;

@Service
public class ProductService {
//  @Autowired
//  private RecipeRepository recipeRepository;

//  public List<Recipe> getRecipes(Integer size, Integer page) {
//    Pageable pageable = PageRequest.of(page, size);
//    return recipeRepository.findAll(pageable).getContent();
//  }
//
//  public Recipe getRecipeByUniqueValue(String uniqueValue) {
//    return recipeRepository.findByUniqueValue(uniqueValue);
//  }
//
//  public Recipe addRecipe(Recipe recipe) {
//    return recipeRepository.save(recipe);
//  }
}